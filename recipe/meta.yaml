{% set name = "apipkg" %}
{% set version = "3.0.0" %}
{% set hash = "f8e504547b753fdff33d126f27b5cfbb5f67864db3ecfdd4bf34aeefb6709bf9" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ hash }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - hatch-vcs
    - hatchling >=0.24
    - python >=3.7
  run:
    - python >=3.7

test:
  imports:
    - apipkg

about:
  home: https://github.com/pytest-dev/apipkg
  dev_url: https://github.com/pytest-dev/apipkg
  license: MIT
  license_file: LICENSE
  summary: With apipkg you can control the exported namespace of a python package and greatly reduce the number of imports for your users.

extra:
  recipe-maintainers:
    - nicoddemus
